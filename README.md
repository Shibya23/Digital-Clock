Files for digital clock on the DE2-115 FPGA board

Has a reset function

Can set hour/minutes using the FPGA push buttons

Includes testbenches 
For the display testbench to work you have to manually drive inputs in modelsim using the 'force' command
